// const INITIAL_STATE = { email: '' };

export default(state = null, action) => {
    //switch statement in Reducer
    switch(action.type) {
        case 'email_changed': 
        default:
            return state; 
    }
};